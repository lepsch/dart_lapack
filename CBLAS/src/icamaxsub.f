      void icamaxsub(n,x,incx, final int iamax) {

      // external icamax
      int      icamax,iamax;
      int     n,incx;
      Complex x(*);

      iamax=icamax(n,x,incx);
      return;
      end;
