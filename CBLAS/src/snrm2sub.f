      void snrm2sub(final int n, final int x, final int incx, final int nrm2,) {

      // external snrm2
      double snrm2,nrm2;
      int     n,incx;
      double x(*);

      nrm2=snrm2(n,x,incx);
      return;
      end;
