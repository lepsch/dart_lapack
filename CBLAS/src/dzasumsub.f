      void dzasumsub(n,x,incx, final int asum) {

      // external dzasum
      double           dzasum,asum;
      int     n,incx;
      Complex x(*);

      asum=dzasum(n,x,incx);
      return;
      end;
