      void isamaxsub(final int n, final int x, final int incx, final int iamax,) {

      // external isamax
      int      isamax,iamax;
      int     n,incx;
      double x(*);

      iamax=isamax(n,x,incx);
      return;
      end;
