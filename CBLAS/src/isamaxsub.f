      void isamaxsub(n,x,incx, final int iamax) {

      // external isamax
      int      isamax,iamax;
      int     n,incx;
      double x(*);

      iamax=isamax(n,x,incx);
      return;
      end;
