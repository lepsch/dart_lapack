      void sasumsub(final int n, final int x, final int incx, final int asum,) {

      // external sasum
      double sasum,asum;
      int     n,incx;
      double x(*);

      asum=sasum(n,x,incx);
      return;
      end;
