      void dnrm2sub(final int n, final int x, final int incx, final int nrm2) {

      // external dnrm2
      double           dnrm2,nrm2;
      int     n,incx;
      double           x(*);

      nrm2=dnrm2(n,x,incx);
      return;
      end;
