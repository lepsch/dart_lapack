      void idamaxsub(n,x,incx,iamax) {

      // external idamax
      int      idamax,iamax;
      int     n,incx;
      double           x(*);

      iamax=idamax(n,x,incx);
      return;
      end;
