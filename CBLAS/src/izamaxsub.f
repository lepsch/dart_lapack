      void izamaxsub(n,x,incx, final int iamax) {

      // external izamax
      int      izamax,iamax;
      int     n,incx;
      Complex x(*);

      iamax=izamax(n,x,incx);
      return;
      end;
