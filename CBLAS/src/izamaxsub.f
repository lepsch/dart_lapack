      void izamaxsub(final int n, final int x, final int incx, final int iamax,) {

      // external izamax
      int      izamax,iamax;
      int     n,incx;
      Complex x(*);

      iamax=izamax(n,x,incx);
      return;
      end;
