      void dasumsub(final int n, final int x, final int incx, final int asum) {

      // external dasum
      double           dasum,asum;
      int     n,incx;
      double           x(*);

      asum=dasum(n,x,incx);
      return;
      end;
