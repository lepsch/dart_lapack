      void scasumsub(final int n, final int x, final int incx, final int asum) {

      // external scasum
      double scasum,asum;
      int     n,incx;
      Complex x(*);

      asum=scasum(n,x,incx);
      return;
      end;
